- insert:
    # The bundle is host-plane installation only. The Codex tool/prompt row
    # lives in presets/codex/agent.cordis.yml, so other presets stay unchanged.
    - id: codex-preset-installer
      name: '@shuind/dsh-codex/installer'
